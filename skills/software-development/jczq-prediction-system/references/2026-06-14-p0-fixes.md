# P0 生产隐患修复 (2026-06-14)

## P0#1: 路由决策不可追溯
- `predict_match_wrapper()` 新增 `r['routing']` 字段记录完整决策链
- 字段: `{'tried': [...], 'selected': 'club'}`

## P0#2: 500.com 全量熔断无兜底
- `_load_fallback_odds()` 加载 `/root/data/odds_history.json`
- 过滤 `start >= today`
- 标记 `std_odds_source='fallback_stale'`

## P0#3: tournament_state 单点故障
- 多级回退链: primary → snapshot → 7天日期扫描 → 空dict
- 自动清理过期快照 (保留7天)

## P0#4: 校准器分布外灾难
- Isotonic 在俱乐部赛也被错误加载(虽然未应用)
- 统一剥离: `_load_calibrators()` 直接 return (None, None)
- 训练 Brier 0.2053 原始概率 vs 校准后 Brier 0.2341→0.2053 回退

### 验证命令
```bash
# 验证路由日志
grep '"routing"' /root/500breaker.log | tail -20

# 验证校准器已剥离
python3 -c "from predict_match import _load_calibrators; print(_load_calibrators())"

# 验证 tournament_state 快照
ls -la /root/data/tournament_state_*.json

# 验证熔断兜底
python3 -c "from daily_jczq import _load_fallback_odds; o=_load_fallback_odds(); print(len(o), o[0].get('std_odds_source','') if o else 'empty')"
```
