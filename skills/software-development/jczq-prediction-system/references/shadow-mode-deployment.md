# Shadow Mode (暗部署) 方法论 (2026-06-30)

## 什么场景用

对一个会影响推荐输出的改动，想先在线上收集数据但不影响用户。

## 做法

- 在结果输出/action 逻辑中**只追加标签不修改原有的流向**
- 例如 `RECOMMEND` → `RECOMMEND [LOW_CONF]`，不 SKIP 不删除
- 数据仍照常落盘到 predictions_log.csv

## 什么时候可以转硬性

- 收集到足够样本（≥100 场带标签数据）
- 对比"带标签 vs 不带标签"组准确率差异稳定（≥2 周）
- 确认新逻辑不会拉低原有指标
